{% extends 'admin/base.html.twig' %}

{% block main %}
<div class="container">
    <div class="row">
        <div class="col-12 text-center">
            <h1>Bienvenue {{ app.user.email }} !!!!</h1>
        </div>
        <div class="col-12">
            <h3>Settings</h3>
        </div>
        <div class="col-12">
            <div class="block-book">
                <a href="{{ path('admin_book_list') }}">Liste par titres</a>
            </div>
            <br>
            <div class="block-book">
                <a href="{{ path('admin_author_list') }}">Lista par auteurs</a>
            </div>
            <br>
            <div class="block-book">
                <a href="{{ path('admin_admin_list') }}">Liste Admin</a><br>
            </div>
        </div>
    </div>
</div>
{% endblock %}